{
  "name": "formly-agent-contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Compile Angular Formly configurations into deterministic, agent-readable contracts.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.23.0",
  "engines": {
    "node": ">=22.13.0 <23",
    "pnpm": "10.23.0"
  },
  "scripts": {
    "app:serve": "pnpm --filter @formly-agent-contracts/formly-test-app serve",
    "audit": "pnpm audit --audit-level=high",
    "build": "pnpm build:demo && pnpm --filter @formly-agent-contracts/formly-test-app build",
    "build:demo": "pnpm --filter @formly-agent-contracts/contract-schema build && pnpm --filter @formly-agent-contracts/formly-adapter build && pnpm --filter @formly-agent-contracts/synthetic-form build && pnpm --filter @formly-agent-contracts/demo-cli build",
    "check": "pnpm lint && pnpm test && pnpm build && pnpm check:demo && pnpm check:docs",
    "check:demo": "node .github/scripts/check-demo.mjs",
    "check:docs": "node .github/scripts/check-docs.mjs",
    "demo": "node scripts/run-demo.mjs",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "22.20.1",
    "eslint": "10.9.1",
    "typescript": "5.9.3",
    "typescript-eslint": "8.68.0",
    "vitest": "4.1.11"
  }
}
