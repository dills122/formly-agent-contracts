{
  "name": "@formly-contract/demo-cli",
  "version": "0.0.0",
  "private": true,
  "description": "Runnable golden-contract inspection demo.",
  "type": "module",
  "scripts": {
    "build": "pnpm typecheck && tsc --project tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@formly-contract/contract-schema": "workspace:*",
    "@formly-contract/formly-adapter": "workspace:*",
    "@formly-contract/synthetic-form": "workspace:*"
  }
}
