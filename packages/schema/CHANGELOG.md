# @formly-contract/schema

## 0.5.0

### Minor Changes

- 9b19f67: Add strict, canonical runtime and dependency provenance contracts for
  versioned workspace execution hosts.
- d41d825: Stop emitting unstable declared DOM-ID locators from Formly array templates and report them with the `UNRELIABLE_DOM_ID` diagnostic.
- 496e1cd: Resolve validated cross-field effects into contracts with field-profile target
  and readiness capabilities, stable rule identities, cycle diagnostics, and
  explicit effect-analysis completeness.
- 4ff045f: Add strict, canonical, versioned cross-field effect registry contracts for application-declared form relationships.
