---
'@formly-contract/schema': minor
'@formly-contract/workspace': minor
'@formly-contract/angular': minor
---

Add guarded per-project Angular JIT workers with import-free discovery,
inventory-before-factory duplicate checks, isolated skip/report behavior,
generation locking, and dependency-snapshot revalidation. Expand compact
field, stepper, choice, repeater, typed-input, and wrapper authoring, and add a
strict proposal-only dynamic-semantics schema, bounded source context packer,
and provider-neutral LLM-0 evaluation lane.
