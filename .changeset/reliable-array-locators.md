---
'@formly-contract/schema': minor
'@formly-contract/compiler': patch
---

Stop emitting unstable declared DOM-ID locators from Formly array templates and report them with the `UNRELIABLE_DOM_ID` diagnostic.
