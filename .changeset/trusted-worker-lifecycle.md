---
'@formly-contract/workspace': minor
---

Harden trusted project workers with direct Node spawning, a scrubbed
environment, read-only permission-model guardrails, persistent lifecycle IPC,
and fail-closed unavailable-isolation selection. Preserve runtime-host identity
failures and deterministic aggregation across reversed worker completion.
